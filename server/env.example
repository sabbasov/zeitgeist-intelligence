# Database Configuration
# Option 1: Direct PostgreSQL connection (for migrations)
# For Supabase: Get this from your Supabase project settings > Database > Connection string > URI tab
# For self-hosted PostgreSQL: postgresql://user:password@localhost:5432/zeitgeist
DATABASE_URL=postgresql://user:password@localhost:5432/zeitgeist

# Option 2: Supabase Client (alternative approach)
# Get these from: Supabase Dashboard → Settings → API
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_anon_key_here
# For admin operations, use service role key (keep secret!)
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Server Configuration
PORT=3001
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# Optional: Stripe Configuration (for purchase verification)
# STRIPE_SECRET_KEY=sk_test_...
